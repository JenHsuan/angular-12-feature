export const getName = () => "demo";

// export type BasicPrimitive = number | string | boolean;

// export function doStuff(value: BasicPrimitive) {
//     if (Math.random() < 0.5) {
//         return undefined;
//     }

//     return value;
// }
